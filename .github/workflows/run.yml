name: V3.0 External Cron Trigger Bot

on:
  workflow_dispatch:
    inputs:
      source:
        description: "Trigger source"
        required: false
        default: "cronjob"
      mode:
        description: "Bot mode (scan, weekly, monthly)"
        required: false
        default: "scan"

concurrency:
  group: signal-bot-execution
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install

      - name: Run Bot
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          DERIV_API_TOKEN: ${{ secrets.DERIV_API_TOKEN }}
          DERIV_APP_ID: ${{ secrets.DERIV_APP_ID }}
          PROXY_URL: ${{ secrets.PROXY_URL }}
          PROXY_SECRET: ${{ secrets.PROXY_SECRET }}
          TRIGGER_SOURCE: ${{ github.event.inputs.source || 'cronjob' }}
          MODE: ${{ github.event.inputs.mode || 'scan' }}
        run: node alert.js

      - name: Commit state and trades update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@github.com"
          git add state.json trades.json
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "Auto update bot state and trade logs"
          git pull --rebase origin main   # ← ADD THIS
          git push origin HEAD:main
